# Вариант 1: одна строка подключения (так Render обычно подставляет её сам
# при привязке базы к сервису — DATABASE_URL имеет приоритет, если задана)
DATABASE_URL=postgresql://alfa_collection_user:<password>@dpg-d4im0jh5pdvs73834210-a.oregon-postgres.render.com:5432/alfa_collection

# Вариант 2: раздельные переменные (используются, если DATABASE_URL не задана)
DB_NAME=alfa_collection
DB_USER=alfa_collection_user
DB_PASSWORD=
DB_HOST=dpg-d4im0jh5pdvs73834210-a.oregon-postgres.render.com
DB_PORT=5432

SECRET_KEY=change-me
